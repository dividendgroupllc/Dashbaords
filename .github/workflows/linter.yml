name: Legacy Linters

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    name: CI owns linting
    runs-on: ubuntu-latest
    steps:
      - name: Print migration notice
        run: echo "Linting and security checks are now part of .github/workflows/ci.yml."
